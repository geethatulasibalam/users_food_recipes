$(document).ready(function(){
	$("body").before("<%= escape_javascript render(:partial=>'reviewform')%>");
	$("#form-modal").modal();
	$('#form-modal').on('hidden.bs.modal', function () {
  	$('#form-modal').trigger("reset");
	})
})